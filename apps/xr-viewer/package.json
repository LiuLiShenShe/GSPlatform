{
  "name": "@gsplatform/xr-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "GSPlatform WebXR Viewer — PlayCanvas Engine + WebXR for immersive Gaussian scene viewing",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playcanvas": "2.22.0"
  },
  "devDependencies": {
    "@playcanvas/splat-transform": "3.3.3",
    "@webgpu/types": "0.1.72",
    "typescript": "~5.8.0",
    "vite": "^6.0.0"
  }
}
